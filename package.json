{
	"name": "Conduit.Cypress.E2ETests",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"cypress:open": "cypress open",
		"cypress:run": "cypress run --browser chrome",
		"cypress:headless": "cypress run --headless --browser chrome",
		"cypress:record": "cypress run --record --headless --browser chrome"
	},
	"keywords": [],
	"author": "developer",
	"license": "ISC",
	"dependencies": {
		"mocha": "^8.1.3"
	},
	"devDependencies": {
		"cypress": "^6.2.0",
		"minimatch": ">=3.0.5",
		"ansi-regex": ">=5.0.1"
	}
}
