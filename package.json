{
	"name": "Conduit.Cypress.E2ETests",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"cypress:open": "cypress open",
		"cypress:run": "cypress run --browser electron",
		"cypress:headless": "cypress run --headless --browser electron",
		"cypress:record": "cypress run --record --headless --browser electron"		
	},
	"keywords": [],
	"author": "developer",
	"license": "ISC",
	"dependencies": {
		"mocha": "^10.2.0"
	},
	"devDependencies": {
		"ansi-regex": ">=5.0.1",
		"cypress": "^6.2.0",
		"minimatch": ">=3.0.5"
	}
}
